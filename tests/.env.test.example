# tests/.env.test.example —— 契约测试的完整测试环境模版（DB 连接 + SSH 隧道）。
#
# 用法：
#   1. 复制本文件为 tests/.env.test（同目录，去掉 .example）——真实文件已在 .gitignore
#      忽略（/tests/.env.test），绝不入库。
#   2. 填入远程测试库的连接值 + SSH 隧道拓扑（把所有 CHANGE_ME 换成真实值）。
#   3. 远程库需有一个能 CREATE SCHEMA / DROP SCHEMA 的测试专用特权角色（`dbllm_test`）。
#      **不需要也不应该有 CREATEROLE/CREATEDB/SUPERUSER/BYPASSRLS/REPLICATION 中任何一项**
#      ——本仓 MUST NOT 执行 CREATE ROLE / GRANT 一类特权 SQL（见 CLAUDE.md「特权 SQL 边界」），
#      留在本仓的契约测试一律零特权 SQL，只自建/自删一次性的 dbllm_fixture schema，
#      不碰任何角色。角色若尚未建，用 tests/provision-test-db.sql 直连远程 PG 的 5432
#      一次性 provision（CREATE DATABASE 在 PgBouncer 6432 上跑不了，必须直连）——该文件同时
#      保留库 owner `dbllm` 的运行时身份（不做任何属性收敛）与新建 `dbllm_test`。
#   4. 跑测试：bash tests/run-contract-test.sh
#      该脚本内置「source 本文件 → 本地端口没监听就自动开隧道 → 跑 pytest」，一条命令搞定。
#      隧道是长活的：脚本只在没开时开一次、开着就复用，不负责关闭；收工手动关：
#        bash shared/ssh-tunnel.sh stop
#
# 契约边界（见 test_db_collect_contract.py 头部 + design.md M25 拍板）：测试 MUST 只认下面
# 这 5 个 DBLLM_TEST_PG* 专属变量，绝不读通用 PGHOST / PGPORT 等（防 shell 已导出值误
# 命中一个非预期的库）。缺任何一个都是硬失败（fail 而非 skip）。
#
# ---- 三个凭据轴（2026-09-10 阶段 0 角色分离，防止后来者把它们当重复真相源合并）----
# 本仓横跨三处口令，指向三个不同用途、生命周期与真相源都不同的账号，长得像但不能互换：
#   1. `.pg-ops/`（运维线，ADR-0002）—— 库 owner `dbllm` 的口令，服务器首次生成、
#      模型 MUST NOT 读；供运维脚本 / `pg-dev-init` 用，固定指向 **dev 服务器**。
#   2. `tests/.env.test`（本文件）—— 测试专用特权角色 `dbllm_test`（PGUSER/PGPASSWORD）
#      与只读角色 `llm_readonly`（RO_USER/RO_PASSWORD）的口令，固定指向**测试库**
#      （当前与 dev 服务器同库，但这是巧合不是契约——见下条）。
#   3. `.dbmeta/.dbllm.env`（只读线，ADR-0006）—— 「大模型要查元数据的那个库」的只读凭据，
#      其指向的环境**不受约束**（dev / 预发 / 生产只读副本均合法）；契约测试的凭据来源
#      与它**解耦**，MUST NOT 从这里读（`test-db-provisioning` R「契约测试以测试角色连接」）。

# ---- 只读角色（run-e2e-smoke.sh 与 tests/test_sql_check_contract.py 读这 2 个）----
# 由 /pg-readonly-setup 供给（generate 出 setup.sql → 人执行 → verify），之后常驻不变。
# 本仓不再手工建 e2e 专用只读角色：llm_readonly 本身就是「非 owner、要穿 PgBouncer 认证面」
# 的角色，旧 dbllm_e2e_ro 的唯一存在理由已被它覆盖（见 provision-test-db.sql §②）。
#
# 这对变量**只服务 pytest 契约测试**（test_sql_check_contract.py）：它要以真只读角色跑
# PREPARE 校验，而 pytest 没法停下等人执行 setup.sql，只能复用一个已存在的角色。
# run-e2e-smoke.sh 自 T71 起**不再读它们**——e2e 走产品真实路径，只读凭据由
# ro-generate.sh 生成并写进它自己那个消费仓的 .dbllm.env，那里才是唯一真相源。
# 填这里的密码 = 你执行 e2e 产出的 setup.sql 时建进库里的那个密码。
DBLLM_TEST_RO_USER=llm_readonly
DBLLM_TEST_RO_PASSWORD=CHANGE_ME

# ---- DB 连接（pytest 读这 5 个；HOST/PORT 指向 SSH 隧道的本地端）----
# 🔴 端口指向 PgBouncer 的 **session 池**（dev 上是 7432），不是 transaction 池。
#    原因：shared/db-collect.sh 用的是会话级 `SET statement_timeout` / `SET lock_timeout`
#    （三段独立隐式事务），transaction 池会把它们分派到与采集载荷不同的后端上，
#    超时守卫静默失效。session 池钉住同一连接，SET 才真生效。
#    （ro-session.sh 走的是 BEGIN + SET LOCAL，两种模式都安全。）
#
# 🔴 PGUSER/PGPASSWORD 是**测试专用特权角色**（`dbllm_test`，tests/provision-test-db.sql
#    §①b 建），不是库 owner `dbllm` 的口令副本——2026-09-10 阶段 0 角色分离后，测试
#    全程以这个角色连接（建删自己的 fixture schema + 跑被测查询），owner 角色只保留其
#    运行时身份，测试代码不再连它。见下方「三个凭据轴」一节。
DBLLM_TEST_PGHOST=127.0.0.1
DBLLM_TEST_PGPORT=17432
DBLLM_TEST_PGDATABASE=dbllm
DBLLM_TEST_PGUSER=dbllm_test
DBLLM_TEST_PGPASSWORD=CHANGE_ME

# ---- SSH 隧道（shared/ssh-tunnel.sh 读这些；localhost:SSH_LOCAL_PORT → 跳板 → SSH_REMOTE_HOST:SSH_REMOTE_PORT）----
# 认证二选一（证书优先）：SSH_KEY_FILE（私钥路径）或 SSH_PASSWORD（需装 sshpass）。
# SSH_LOCAL_PORT 要与上面的 DBLLM_TEST_PGPORT 一致；本机端口刻意不与远端同号，避开本机占用。
# SSH_KEY_FILE 用 ~/ 而非 $HOME/：本文件被 source（两种写法都展开），但 .dbmeta/.dbllm.env
# 是被逐行解析的（ADR-0006，$VAR 留字面量），两处写成 ~/ 才真能互相照抄。
SSH_LOCAL_PORT=17432
SSH_HOST=CHANGE_ME
SSH_PORT=22
SSH_USER=root
SSH_REMOTE_HOST=127.0.0.1
SSH_REMOTE_PORT=7432
SSH_KEY_FILE=~/.ssh/CHANGE_ME.pem
#SSH_PASSWORD=CHANGE_ME
